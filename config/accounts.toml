# X accounts to mirror in full (posts + replies).
# Account mirrors are optional. `bowerbird init` can offer public example
# accounts during setup, but the source repo does not pre-enable any account
# mirrors or ship pre-ingested account posts.
