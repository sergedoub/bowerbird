/** @type {import('next').NextConfig} */
const nextConfig = {
  // The app talks only to the GitHub API (and later X/Anthropic/Slack) server-side.
  // No image optimization or special features needed; keep defaults.
};

export default nextConfig;
